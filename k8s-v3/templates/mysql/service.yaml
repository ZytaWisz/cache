apiVersion: v1
kind: Service

metadata:
  name: {{ .Values.mysql.name }}
  namespace: {{ .Values.namespace }}

  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.syncWaves.mysql }}"

spec:
  clusterIP: None

  selector:
    app: {{ .Values.mysql.name }}

  ports:
    - port: {{ .Values.mysql.port }}

      targetPort: {{ .Values.mysql.port }}